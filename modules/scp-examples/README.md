# SCP examples

JSON de referência, não um módulo Terraform (sem `main.tf`/`variables.tf` —
por isso este diretório **não** entra nas matrices de
`validate`/`tflint`/`test` do CI). Ver
[`docs/multi-account-migration.md`](../../docs/multi-account-migration.md)
para o contexto de quando e como aplicar cada uma.

- `deny-outside-allowed-regions.json`
- `deny-disable-cloudtrail.json`
